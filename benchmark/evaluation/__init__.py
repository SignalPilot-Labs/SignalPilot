"""dbt benchmark evaluation against gold-standard DuckDB databases.

Submodules:
    db_utils    — find_result_db, row counts, sampling, precomputed-table detection
    comparator  — evaluate() and the Spider2-compatible DataFrame comparator
"""
