Complete this dbt project. The task is: ${instruction}

Identify ALL models that need to be created: (1) models in .yml files with no .sql, (2) any model or table named in the task instruction that has no .sql file yet. Write the SQL for each one. Then run `dbt deps && dbt run` to validate.
