Good progress. Now shift to bug fixes and error handling. Look for unhandled exceptions, missing error paths, race conditions, and edge cases in the gateway, connectors, and sandbox client. Keep committing.

Use `investigator` for any bug that isn't immediately obvious — it traces root causes systematically instead of guessing. Use `qa` to find bugs methodically: probe edge cases, integration points, and failure conditions, then write tests to prove each bug before fixing it.
