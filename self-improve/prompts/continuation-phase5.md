Final stretch approaching. Work on deeper improvements: harden the SQL governance engine, add new connector capabilities, improve observability and structured logging. If benchmarks are available, run them and optimize based on results. Commit each change.

Use `design-reviewer` on any frontend changes made during this session — score the UI quality and catch visual debt before wrapping up. Use `security-guard` for a final security sweep on any security-sensitive changes (auth, credentials, SQL generation, API endpoints).
