"use client";

/**
 * Shared rendered-markdown block for chat surfaces. The renderer lives in
 * `components/chat/markdown/`; this module keeps the import path the chat
 * surfaces already use.
 */
export { ChatMarkdown } from "~/components/chat/markdown";
