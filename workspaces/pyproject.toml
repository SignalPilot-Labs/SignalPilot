[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "workspaces"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
only-include = []
